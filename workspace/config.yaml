# 硅基流动 (SiliconFlow) — OpenAI 兼容端点
# key 在 workspace/.env 的 OPENAI_API_KEY（不回显）
model:
  provider: custom
  default: Qwen/Qwen3-32B
  base_url: https://api.siliconflow.cn/v1

platforms:
  api_server:
    port: 8642
    enabled: true
mcp_servers: {}
